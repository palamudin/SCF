# SCF Agent Notes

- Do not use Unity MCP from VS Code sessions for this project. The bridge approval/connection flow is unreliable here, so treat MCP as unavailable.
- Work through local filesystem inspection/edits and user-provided Unity console output instead.
